

block content
  h1 Your Orders

  each order in viewOpenOrders
    form(method="POST" action="/cart/delete/")
      button(type="submit") Clear Shopping Cart
    each product in order.Products
      ul
        li= `${product.title}`
        li= `${product.price}`