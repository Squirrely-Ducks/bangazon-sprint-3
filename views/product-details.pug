extends index.pug

block content
  .display-card
    h2 Product Details
    h3 Title:  #{product.title}
    img(src="http://via.placeholder.com/200x300" alt="Placeholder")
    h4 Price: $#{product.price.slice(0,-2)}.#{product.price.slice(-2)}
    h4 Description: #{product.description}
    h5 Quantity: #{product.quantity}
